# Ridanexpress-Marketplace-
# NOTE: this is a private project and should not be disclosed to the public for security reasons

*npm install -{ to install the required dependencies}
*npm run server -{to run the server}
